import LoadingProvider from "@/components/loadingProvider";

export default function Home() {
  return (
    <LoadingProvider>
      <section>
        <h1>init</h1>
      </section>
    </LoadingProvider>
  );
}
